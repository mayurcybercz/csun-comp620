#include <stdio.h>
#include <string.h>

int main(){
    char str1[]="123";
    int str1_len=strlen(str1);
    char str2[]="456";
    int str2_len=strlen(str2);
    printf("First number is %s of length %d \n",str1,str1_len);
    printf("Second number is %s of length %d\n",str2,str2_len);
    int res_len=str1_len+str2_len;
    char res[res_len];

    for(int )


    return 0;
}
